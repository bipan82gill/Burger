<div class = "container">
  <div class = "row">
    <div class = "col-lg-12">
      <div class="jumbotron">
          <h1 class="display-4">Eat-Da-Burger</h1>
          <p class="lead">Are To Ready To Order Your Burger?</p>
          <hr class="my-4">
          
          <p class="lead">
          <form class="create-form">
              <div class="form-group">
                  <label for="bur">Burger:</label>
                      <input type="text" id="bur" name="burger_name">
                      <button type="submit"class="btn btn-primary btn-lg">Order it</button>
              </div>
          </form>
          </p>
        </div>
    </div>
  </div>
</div>
    
      {{!-- <img src= "./assests/img/burger.jpg" heigth="100px">
    </div> --}}
    {{!-- <div class = col-md-2></div> --}}

<div class="row">
    <div class = "col-md-4">
        <h2>Ready to Eat Burger?</h2>
        <ul>
              {{#each burgers}}
                {{#unless this.devoured}}
                <li>
                  {{this.burger_name}}
                 
                  <button id="devoureBtn" data-value ="true"id ="check" class="btn btn-warning btn-sm" data-id={{this.id}}> Davoure it </button>
                
                </li>
                {{/unless}}
              {{/each}}
        </ul>
    </div>
    <div class = col-md-4></div>
    <div class = "col-md-4">
        <h2>Enjoyed Burger!</h2>
              <ul>
              {{#each burgers}}
                {{#if this.devoured}}
                  <li>{{this.burger_name}}
                    <button id="deleteBtn" data-value="false" data-id={{this.id}}>Delete</button>
                  </li>
                {{/if}}
              </li></div>
              {{/each}}
            </ul>
    </div>
  </div>

</div>
